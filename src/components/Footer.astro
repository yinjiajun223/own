---
import { footerText } from "../data/content";
---

<footer>
  <div class="group">
    <p>{footerText}</p>
    <a href="https://beian.miit.gov.cn/" target="_blank">鄂ICP备2024088932号-1</a>
  </div>
</footer>
<style>
  footer {
    display: flex;
    flex-direction: column;
    gap: 3rem;
    margin-top: 4rem;
    padding: 3rem 2rem 3rem;
    text-align: center;
    color: var(--color-400);
    font-size: var(--text-sm);
  }

  footer a {
    color: var(--color-400);
    text-decoration: 1px solid underline transparent;
    text-underline-offset: 0.25em;
    transition: text-decoration-color var(--theme-transition);
  }

  footer a:hover,
  footer a:focus {
    text-decoration-color: currentColor;
  }

  .group {
    display: block;
    width: 100%;
    text-align: center;
  }
  .group p {
    text-align: center;
  }

  .socials {
    display: flex;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
  }
</style>

<script>
  //======================================================
  // add ref
  //======================================================
  // 获取所有带有 https 的 a 标签
  // const httpsLinks = document.querySelectorAll('a[href^="https://"]');
  // // 遍历所有带有 https 的 a 标签
  // httpsLinks.forEach(link => {
  //   // 在 href 属性的末尾添加 ?ref=123
  //   const newHref = link.href + '?ref=606design.art';
  //   // 更新 a 标签的 href 属性
  //   link.href = newHref;
  // });
</script>
